import { Link } from "react-router-dom";

const Planets = () => {
    return (
        <section>
            <h2>Planets</h2>
            <Link to="/">Back Home</Link>
        </section>
    );
};

export default Planets;
