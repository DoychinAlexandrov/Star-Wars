import { Link } from "react-router-dom";

const Species = () => {
    return (
        <section>
            <h2>Species</h2>
            <Link to="/">Back Home</Link>
        </section>
    );
};

export default Species;
