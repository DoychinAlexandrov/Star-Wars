import { Link } from "react-router-dom";

const Spaceships = () => {
    return (
        <section>
            <h2>Spaceships</h2>
            <Link to="/">Back Home</Link>
        </section>
    );
};

export default Spaceships;
